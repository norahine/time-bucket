<!DOCTYPE html>
<html>
  <head>
    <title>TimeBucket</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <script defer src="https://use.fontawesome.com/releases/v5.0.6/js/all.js"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="#">Time Bucket</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "Activities", activities_path %>
            </li>
            <li class="nav-item">
              <a href="#">Tracking</a>
            </li>
            <li class="nav-item">
              <a href="#">Reports</a>
            </li>
            <li class="nav-item">
              <a href="#">Profile Management<i class="far fa-user"></i></a>
            </li>
            <li class="nav-item">
              <%= link_to "Logout", destroy_user_session_path, method: :delete %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Login", new_user_session_path %>
            </li>
          <% end %>
        </ul>
      </div>
    </nav>

    <% [:success, :alert, :notice, :error].each do |type| %>
      <% if flash[type].present? %>
        <p class="alert <%= bootstrap_class_for_alert(type) %>"><%= flash[type] %></p>
      <% end %>
    <% end %>

    <%= yield %>

    <footer class="container-fluid">
      <div class="row">
        <div class="col-sm-5 offset-5">
          <%= link_to "About", "#" %>
          <%= link_to "Contact", "#" %>
          <%= link_to "FAQ", "#" %>
        </div>
        <div class="col-sm-2 text-right">
          <span>&copy; Natalia Barańska, <%= Time.current.year %></span>
        </div>
      </div>
    </footer>
  </body>
</html>
